machine:
  # kubelet:
  #   extraMounts:
  #     - destination: /mnt/other
  #       type: bind
  #       source: /mnt/other
  #       options:
  #         - bind
  #         - rshared
  #         - rw
  #     - destination: /mnt/main
  #       type: bind
  #       source: /mnt/main
  #       options:
  #         - bind
  #         - rshared
  #         - rw
  # disks:
  #     - device: /dev/sdb
  #       partitions:
  #         - mountpoint: /mnt/main
  #     - device: /dev/sdc
  #       partitions:
  #         - mountpoint: /mnt/other
  kernel:
    modules:
      - name: nvidia
      - name: nvidia_uvm
      - name: nvidia_drm
      - name: nvidia_modeset
  sysctls:
    net.core.bpf_jit_harden: 1
